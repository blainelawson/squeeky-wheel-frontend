const initialState = {
    username: "",
    password: "",
    zipcode: ""
}

export default (state = initialState, action) => {
    switch (action.type) {
        default: 
            return state
    }
}